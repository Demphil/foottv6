name: Update Matches Data

on:
  schedule:
    - cron: '0 */3 * * *'  # كل 3 ساعات
  workflow_dispatch:

jobs:
  update-matches:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
        with:
          persist-credentials: true
          fetch-depth: 0

      - name: Configure Git
        run: |
          git config --global user.name "GitHub Actions"
          git config --global user.email "actions@github.com"

      - name: Create Matches Data
        run: |
          mkdir -p data
          cat << 'EOF' > data/matches.json
          {
            "today": [
              {
                "id": "match-$(date +%s)",
                "homeTeam": {
                  "name": "الهلال",
                  "logo": "/images/teams/alhilal.png"
                },
                "awayTeam": {
                  "name": "النصر",
                  "logo": "/images/teams/alnassr.png"
                },
                "score": "VS",
                "time": "21:00",
                "date": "$(date -u +'%Y-%m-%dT21:00:00Z')",
                "league": {
                  "name": "الدوري السعودي",
                  "logo": "/images/leagues/spl.png"
                },
                "channels": ["bein SPORTS HD1"],
                "status": "قادم"
              }
            ],
            "tomorrow": [],
            "updatedAt": "$(date -u +'%Y-%m-%dT%H:%M:%SZ')"
          }
          EOF

      - name: Commit & Push Changes
        run: |
          git add data/matches.json
          git diff --cached --quiet || git commit -m "🔄 تحديث بيانات المباريات [auto]"
          git pull --rebase
          git push
